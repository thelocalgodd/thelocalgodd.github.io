---
const { title, date, post } = Astro.props;
---

<a
  href=`${post}`
  target="_self"
  class="list-none flex justify-between w-screen md:mx-auto md:w-[800px] text-sm my-2 border-l border-r border-blue-200 bg-blue-100 bg-opacity-0 px-2 dark:border-zinc-600 dark:bg-zinc-600 dark:bg-opacity-0 dark:text-zinc-300"
>
  <p class="bg-inherit bg-opacity-50">{title}</p>
  <p class="bg-inherit bg-opacity-50">{date}</p>
</a>
