---
import Tag from "./Tag.astro";

const { title, description, link, tag } = Astro.props;
---

<a href={link} target="_blank">
  <div
    id="project"
    class="border border-solid hover:border-dashed text-sm p-4 w-[320px] md:w-[250px] my-2 rounded-lg flex flex-col border-blue-300 bg-blue-100 bg-opacity-0 px-2 dark:border-zinc-500 dark:bg-zinc-200 dark:bg-opacity-0 dark:text-zinc-500"
  >
    <p class="text-zinc-600 dark:text-zinc-400">
      {title}
    </p>
    <hr class="border-blue-200 my-2 dark:border-zinc-500" />
    <p class="my-2">{description}</p>
    <!-- <img
      src="/ss.png"
      class="rounded-xl -mb-10 h-16 w-[300px] object-cover border"
      alt="image"
    /> -->
    <!-- <div class="flex flex-row flex-wrap">
      <Tag name={tag} />
    </div> -->
  </div>
</a>
